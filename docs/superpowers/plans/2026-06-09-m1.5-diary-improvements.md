# M1.5 日記運用改良 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** バックデート編集・削除・Markdown 描画・Markdown 編集プレビュー・連続記入日数表示の 5 機能を、既存 M1 CRUD の上に最小工数で追加する。

**Architecture:** view/edit の切替は URL クエリではなく `DiaryEditor` 内の shadcn Tabs（編集/プレビュー）で実現。Markdown は `react-markdown + remark-gfm + @tailwindcss/typography` を採用。削除は Server Action（`redirect` ではなく `router.push` で navigate）。streak は純粋関数として `lib/diary/streak.ts` に分離。

**Tech Stack:** Next.js 16 / shadcn Tabs (base-nova) / react-markdown / remark-gfm / @tailwindcss/typography / 既存 Drizzle・Zod・Server Actions パターン

**Source spec:** [`docs/superpowers/specs/2026-06-09-m1.5-diary-improvements-design.md`](../specs/2026-06-09-m1.5-diary-improvements-design.md)

**前提:** M0〜M3 完了。シーダー導入済み。`/history` がカレンダー化済み。Tailwind v4 + shadcn `base-nova` スタイルで動作。

---

## 関係ファイル全体図

```
diary-app/
├── lib/
│   ├── actions/diary.ts                # MODIFY: deleteDiaryEntry 追加
│   └── diary/streak.ts                 # NEW: 連続日数の純粋関数
├── components/
│   ├── diary/
│   │   ├── DiaryEditor.tsx             # MODIFY: Tabs + 削除ボタン + defaultTab prop
│   │   ├── DiaryMarkdown.tsx           # NEW: react-markdown ラッパー
│   │   └── DiaryCalendar.tsx           # MODIFY: 過去未記入セルをリンク化
│   └── ui/tabs.tsx                     # NEW: shadcn add tabs で生成
├── app/
│   ├── page.tsx                        # MODIFY: streak 表示
│   ├── diary/[date]/page.tsx           # MODIFY: 未来日付404 + バックデート + defaultTab
│   └── globals.css                     # MODIFY: @plugin "@tailwindcss/typography"
└── package.json                        # MODIFY: react-markdown / remark-gfm / @tailwindcss/typography
```

依存方向:
- `lib/diary/streak.ts` ← `app/page.tsx`
- `components/diary/DiaryMarkdown.tsx` ← `components/diary/DiaryEditor.tsx`
- `lib/actions/diary.ts (deleteDiaryEntry)` ← `components/diary/DiaryEditor.tsx`

---

## Task 1: 依存パッケージ導入

**目的:** shadcn Tabs、Markdown 関連、Typography プラグインを揃える。

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`
- Create: `components/ui/tabs.tsx` (shadcn add で生成)
- Modify: `app/globals.css`

- [ ] **Step 1: shadcn Tabs を追加**

```bash
pnpm dlx shadcn@latest add tabs
```

Expected: `components/ui/tabs.tsx` が生成される。必要な Radix/Base UI の依存も自動追加。

- [ ] **Step 2: Markdown ライブラリと Typography プラグイン**

```bash
pnpm add react-markdown remark-gfm
pnpm add -D @tailwindcss/typography
```

Expected: 
- `dependencies` に `react-markdown` (v9.x) と `remark-gfm` (v4.x) が追加
- `devDependencies` に `@tailwindcss/typography` (v0.5.x) が追加

- [ ] **Step 3: app/globals.css に typography プラグイン宣言を追記**

`app/globals.css` の `@import "tailwindcss";` 直後の行に以下を追加（`@plugin "tw-animate-css";` などの既存 `@plugin` 宣言がある場所と同じブロック）:

```css
@plugin "@tailwindcss/typography";
```

> 注: ファイル先頭付近、`@import` の後・`@theme inline` の前に置く。既存の `@plugin` 行があればその近くで OK。

- [ ] **Step 4: 型チェックとビルド確認**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

- [ ] **Step 5: コミット**

```bash
git add components/ui/tabs.tsx app/globals.css package.json pnpm-lock.yaml
git commit -m "chore: install shadcn tabs, react-markdown, remark-gfm, typography plugin"
```

---

## Task 2: 連続記入日数の純粋関数

**目的:** `computeStreak(entryDates, today)` 関数を実装し、トップページで使えるようにする。

**Files:**
- Create: `lib/diary/streak.ts`

- [ ] **Step 1: lib/diary/streak.ts を作成**

```bash
mkdir -p lib/diary
```

```typescript
// lib/diary/streak.ts
// 連続記入日数を求める純粋関数。
// 「今日」または「昨日」を起点に遡り、連続している日数を返す。
// 今日も昨日も未記入なら 0。

function pad2(n: number): string {
  return String(n).padStart(2, '0');
}

function subDays(yyyymmdd: string, days: number): string {
  const [y, m, d] = yyyymmdd.split('-').map(Number);
  const date = new Date(Date.UTC(y, m - 1, d));
  date.setUTCDate(date.getUTCDate() - days);
  const y2 = date.getUTCFullYear();
  const m2 = pad2(date.getUTCMonth() + 1);
  const d2 = pad2(date.getUTCDate());
  return `${y2}-${m2}-${d2}`;
}

export function computeStreak(entryDates: readonly string[], today: string): number {
  const dates = new Set(entryDates);

  // 起点: 今日が記入済みなら今日、未記入なら昨日
  let cursor = dates.has(today) ? today : subDays(today, 1);

  if (!dates.has(cursor)) {
    return 0; // 今日も昨日も書いてない
  }

  let count = 0;
  while (dates.has(cursor)) {
    count += 1;
    cursor = subDays(cursor, 1);
  }
  return count;
}
```

設計意図:
- `lib/calendar/month-grid.ts` と同じ「文字列ベースで日付演算」パターンを採用（TZ ハマり回避）
- `Set` で O(1) 引き当て
- `readonly string[]` で入力配列を改変しないことを型で示す

- [ ] **Step 2: スモークテスト**

```bash
pnpm exec tsx -e "
import { computeStreak } from './lib/diary/streak';

function assertEq(actual, expected, label) {
  if (actual !== expected) {
    console.error('FAIL:', label, 'expected', expected, 'got', actual);
    process.exit(1);
  }
  console.log('PASS:', label, '=', actual);
}

// 今日も昨日も書いてない
assertEq(computeStreak([], '2026-06-09'), 0, '空配列');
assertEq(computeStreak(['2026-06-07'], '2026-06-09'), 0, '一昨日のみ');

// 今日記入済み、連続
assertEq(computeStreak(['2026-06-09'], '2026-06-09'), 1, '今日のみ');
assertEq(computeStreak(['2026-06-09','2026-06-08'], '2026-06-09'), 2, '今日と昨日');
assertEq(computeStreak(['2026-06-09','2026-06-08','2026-06-07'], '2026-06-09'), 3, '3日連続');

// 今日未記入、昨日から遡る
assertEq(computeStreak(['2026-06-08'], '2026-06-09'), 1, '昨日のみ');
assertEq(computeStreak(['2026-06-08','2026-06-07'], '2026-06-09'), 2, '昨日と一昨日');

// 途中で抜けあり
assertEq(computeStreak(['2026-06-09','2026-06-08','2026-06-06'], '2026-06-09'), 2, '今日と昨日、その前抜け');
assertEq(computeStreak(['2026-06-09','2026-06-07'], '2026-06-09'), 1, '今日のみ、昨日抜け');

// 順序不定でも動く
assertEq(computeStreak(['2026-06-07','2026-06-09','2026-06-08'], '2026-06-09'), 3, '順不同');

console.log('\\nAll tests passed.');
"
```

Expected: すべて PASS、最後に `All tests passed.` が出る。

- [ ] **Step 3: コミット**

```bash
git add lib/diary/streak.ts
git commit -m "feat: add computeStreak pure function for diary streak counting"
```

---

## Task 3: DiaryMarkdown コンポーネント

**目的:** Markdown 文字列を `prose` クラスで描画する単機能コンポーネント。

**Files:**
- Create: `components/diary/DiaryMarkdown.tsx`

- [ ] **Step 1: components/diary/DiaryMarkdown.tsx を作成**

```tsx
// components/diary/DiaryMarkdown.tsx
import Markdown from 'react-markdown';
import remarkGfm from 'remark-gfm';

interface DiaryMarkdownProps {
  content: string;
}

export function DiaryMarkdown({ content }: DiaryMarkdownProps) {
  if (content.trim().length === 0) {
    return (
      <p className="text-sm text-muted-foreground">
        プレビューする内容がありません。
      </p>
    );
  }

  return (
    <div className="prose prose-neutral dark:prose-invert max-w-none">
      <Markdown remarkPlugins={[remarkGfm]}>{content}</Markdown>
    </div>
  );
}
```

設計意図:
- Server Component（`'use client'` 不要、状態を持たない）
- 空文字のときは empty state を表示（プレビュータブの初期状態が空白にならないように）
- `prose-neutral` + `dark:prose-invert` で shadcn neutral テーマと整合
- `max-w-none` で `prose` のデフォルト `max-width` 制限を外す（親 container に任せる）

- [ ] **Step 2: 型チェック**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

- [ ] **Step 3: コミット**

```bash
git add components/diary/DiaryMarkdown.tsx
git commit -m "feat: add DiaryMarkdown component using react-markdown and remark-gfm"
```

---

## Task 4: deleteDiaryEntry Server Action

**目的:** 既存 `lib/actions/diary.ts` に削除 Server Action を追加する。

**Files:**
- Modify: `lib/actions/diary.ts`

- [ ] **Step 1: lib/actions/diary.ts を編集**

ファイル末尾に以下を追加（`saveDiaryEntry` の後）:

```typescript
export type DeleteResult =
  | { ok: true }
  | { ok: false; error: string };

export async function deleteDiaryEntry(entryDate: string): Promise<DeleteResult> {
  if (!/^\d{4}-\d{2}-\d{2}$/.test(entryDate)) {
    return { ok: false, error: '日付の形式が不正です' };
  }

  try {
    await db
      .delete(diaryEntries)
      .where(and(eq(diaryEntries.userId, USER_ID), eq(diaryEntries.entryDate, entryDate)));

    revalidatePath('/');
    revalidatePath('/history');
    revalidatePath(`/diary/${entryDate}`);

    return { ok: true };
  } catch (err) {
    console.error('Failed to delete diary entry:', err);
    return { ok: false, error: '削除に失敗しました' };
  }
}
```

import が不足する場合は先頭の import 文に追加:
- `and, eq` は既存
- `db, diaryEntries` は既存
- `revalidatePath` は既存

import 行で `and, eq` を改めて追加する必要はない（`saveDiaryEntry` 用に既に import 済み）。

- [ ] **Step 2: 型チェック**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

- [ ] **Step 3: コミット**

```bash
git add lib/actions/diary.ts
git commit -m "feat: add deleteDiaryEntry server action with date format guard"
```

---

## Task 5: DiaryEditor リファクタ — Tabs + 削除ボタン + defaultTab

**目的:** 既存 DiaryEditor に「編集/プレビュー」タブ、削除ボタン、`defaultTab` prop を追加。

**Files:**
- Modify: `components/diary/DiaryEditor.tsx`

- [ ] **Step 1: components/diary/DiaryEditor.tsx を全面置き換え**

ファイル全体を以下に置き換える:

```tsx
// components/diary/DiaryEditor.tsx
'use client';

import { useState, useTransition } from 'react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Textarea } from '@/components/ui/textarea';
import { deleteDiaryEntry, saveDiaryEntry } from '@/lib/actions/diary';
import { DiaryMarkdown } from '@/components/diary/DiaryMarkdown';

interface DiaryEditorProps {
  entryDate: string;
  initialContent?: string;
  defaultTab?: 'edit' | 'preview';
}

export function DiaryEditor({
  entryDate,
  initialContent = '',
  defaultTab = 'edit',
}: DiaryEditorProps) {
  const router = useRouter();
  const [content, setContent] = useState(initialContent);
  const [isSavePending, startSaveTransition] = useTransition();
  const [isDeletePending, startDeleteTransition] = useTransition();
  const [feedback, setFeedback] = useState<
    { kind: 'success' | 'error'; message: string } | null
  >(null);

  const handleAction = (formData: FormData) => {
    startSaveTransition(async () => {
      const result = await saveDiaryEntry(formData);
      if (result.ok) {
        setFeedback({ kind: 'success', message: '保存しました' });
      } else {
        setFeedback({ kind: 'error', message: result.error });
      }
    });
  };

  const handleDelete = () => {
    if (!window.confirm('この日記を削除しますか？')) return;
    startDeleteTransition(async () => {
      const result = await deleteDiaryEntry(entryDate);
      if (result.ok) {
        router.push('/');
      } else {
        setFeedback({ kind: 'error', message: result.error });
      }
    });
  };

  const canDelete = initialContent.trim().length > 0;
  const isPending = isSavePending || isDeletePending;

  return (
    <form action={handleAction} className="space-y-4">
      {/* form データは hidden input から確実に渡す。タブ切替で textarea が unmount される可能性に備える */}
      <input type="hidden" name="entryDate" value={entryDate} />
      <input type="hidden" name="content" value={content} />

      <Tabs defaultValue={defaultTab} className="w-full">
        <TabsList>
          <TabsTrigger value="edit">編集</TabsTrigger>
          <TabsTrigger value="preview">プレビュー</TabsTrigger>
        </TabsList>

        <TabsContent value="edit">
          <Textarea
            value={content}
            onChange={(e) => setContent(e.target.value)}
            rows={15}
            placeholder="今日はどんな1日でしたか？"
            className="w-full"
          />
        </TabsContent>

        <TabsContent value="preview">
          <div className="min-h-[15rem] rounded-md border p-4">
            <DiaryMarkdown content={content} />
          </div>
        </TabsContent>
      </Tabs>

      <div className="flex items-center gap-3">
        <Button type="submit" disabled={isPending || content.trim().length === 0}>
          {isSavePending ? '保存中…' : '保存'}
        </Button>
        {canDelete && (
          <Button
            type="button"
            variant="destructive"
            onClick={handleDelete}
            disabled={isPending}
          >
            {isDeletePending ? '削除中…' : '削除'}
          </Button>
        )}
        {feedback && (
          <span
            className={
              feedback.kind === 'success'
                ? 'text-sm text-green-600 dark:text-green-400'
                : 'text-sm text-destructive'
            }
          >
            {feedback.message}
          </span>
        )}
      </div>
    </form>
  );
}
```

主な変更点:
- `defaultTab` prop 追加（オプショナル、デフォルト `'edit'`）→ 既存呼び出しに後方互換
- 編集/プレビュー の Tabs を内蔵
- form データは `<input type="hidden" name="content">` で送るので、タブ切替で textarea が一時的に unmount されてもデータ喪失しない
- `Textarea` から `name="content"` を外した（hidden input が役割を担う）
- 削除ボタンは `canDelete`（既存 content がある時）だけ表示
- 削除中・保存中の状態を別々に管理（`isSavePending` / `isDeletePending`）し、操作中はもう片方も disabled
- 削除成功時は `router.push('/')` で navigation

- [ ] **Step 2: 型チェック**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

`Button` の `variant="destructive"` がない場合はビルドエラー。base-nova スタイルでは `destructive` variant が含まれているはずだが、無い場合は `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"` を直接渡す形にフォールバック。

- [ ] **Step 3: コミット**

```bash
git add components/diary/DiaryEditor.tsx
git commit -m "feat: refactor DiaryEditor with edit/preview tabs and delete button"
```

---

## Task 6: DiaryCalendar — 過去未記入セルをリンク化

**目的:** バックデート編集の入口として、過去かつ未記入のセルをクリック可能にする。

**Files:**
- Modify: `components/diary/DiaryCalendar.tsx`

- [ ] **Step 1: DayCell コンポーネント内の「未記入の過去日」ブランチをリンク化**

現状の DayCell の末尾近くにある「Past day without entry」のブランチ:

```tsx
// 既存（変更前）
if (cell.isToday) { /* Link to / */ }

// Unwritten past day
return <div className={className}>{cell.day}</div>;
```

これを以下に変更:

```tsx
if (cell.isToday) { /* Link to / は既存のまま */ }

// Unwritten past day (clickable to backdate)
return (
  <Link
    href={`/diary/${cell.iso}`}
    className={className}
    aria-label={`${cell.iso} に日記を書く`}
  >
    {cell.day}
  </Link>
);
```

また、`cellClasses` 関数内の「Past day without an entry」のブランチに hover スタイルを追加:

```tsx
// Past day without an entry (now clickable)
if (cell.isToday) {
  return `${base} ring-2 ring-primary text-foreground hover:bg-accent transition`;
}
return `${base} text-muted-foreground hover:bg-accent transition`;
```

つまり既存:
```tsx
return `${base} text-muted-foreground`;
```
を
```tsx
return `${base} text-muted-foreground hover:bg-accent transition`;
```
に変更（hover で背景が浮くようにし、クリック可能感を出す）。

- [ ] **Step 2: 凡例も更新（必要に応じて）**

`書かなかった日` の凡例には今クリック可能であることを示唆するため、コメント変更は不要だが、凡例の説明テキストを `書かなかった日（クリックで書ける）` に変えるかは好みで。**M1.5 では変えない**（凡例が長くなるのを避け、シンプルさを優先）。

- [ ] **Step 3: 型チェック**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

- [ ] **Step 4: コミット**

```bash
git add components/diary/DiaryCalendar.tsx
git commit -m "feat: make past unwritten calendar cells clickable for backdate editing"
```

---

## Task 7: /diary/[date] ページ — バックデート + defaultTab

**目的:** 未来日付は 404、過去日付は entry の有無で defaultTab を切り替える。

**Files:**
- Modify: `app/diary/[date]/page.tsx`

- [ ] **Step 1: app/diary/[date]/page.tsx を全面置き換え**

```tsx
// app/diary/[date]/page.tsx
import { notFound } from 'next/navigation';
import { DiaryEditor } from '@/components/diary/DiaryEditor';
import { getDiaryEntry } from '@/lib/db/queries/diary';
import { todayInTokyo } from '@/lib/calendar/month-grid';

const DATE_PATTERN = /^\d{4}-\d{2}-\d{2}$/;

interface PageProps {
  params: Promise<{ date: string }>;
}

export default async function DiaryDetailPage({ params }: PageProps) {
  const { date } = await params;
  if (!DATE_PATTERN.test(date)) {
    notFound();
  }

  const today = todayInTokyo();
  if (date > today) {
    notFound(); // 未来日付は 404
  }

  const entry = await getDiaryEntry(date);
  const initialContent = entry?.content ?? '';
  const defaultTab = entry ? 'preview' : 'edit';

  return (
    <main className="container mx-auto max-w-3xl p-6">
      <h1 className="mb-6 text-2xl font-bold">{date}</h1>
      <DiaryEditor
        entryDate={date}
        initialContent={initialContent}
        defaultTab={defaultTab}
      />
    </main>
  );
}
```

設計意図:
- 日付不正・未来日付は 404 のまま
- 過去 + entry あり → preview タブ既定
- 過去 + entry なし → edit タブ既定（バックデート）
- 既存の「entry なしで `notFound()`」ロジックは削除

- [ ] **Step 2: 型チェック**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

- [ ] **Step 3: コミット**

```bash
git add app/diary/[date]/page.tsx
git commit -m "feat: support backdate editing and preview default on diary detail page"
```

---

## Task 8: app/page.tsx — streak 表示

**目的:** トップに「🔥 N 日連続記入中」を表示し、エディタを編集タブ既定にする。

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: app/page.tsx を全面置き換え**

```tsx
// app/page.tsx
import { DiaryEditor } from '@/components/diary/DiaryEditor';
import { getDiaryEntry, listDiaryEntries } from '@/lib/db/queries/diary';
import { computeStreak } from '@/lib/diary/streak';
import { todayInTokyo } from '@/lib/calendar/month-grid';

export default async function HomePage() {
  const date = todayInTokyo();
  const [existing, allEntries] = await Promise.all([
    getDiaryEntry(date),
    listDiaryEntries(),
  ]);
  const streak = computeStreak(
    allEntries.map((e) => e.entryDate),
    date,
  );

  return (
    <main className="container mx-auto max-w-3xl p-6">
      <div className="mb-6 flex flex-wrap items-baseline gap-3">
        <h1 className="text-2xl font-bold">{date} の日記</h1>
        {streak > 0 && (
          <span className="rounded-full bg-accent px-3 py-1 text-sm font-medium">
            🔥 {streak}日連続記入中
          </span>
        )}
      </div>
      <DiaryEditor
        entryDate={date}
        initialContent={existing?.content ?? ''}
        defaultTab="edit"
      />
    </main>
  );
}
```

主な変更点:
- `listDiaryEntries()` も並列取得
- `computeStreak` で streak 計算
- streak > 0 のときだけバッジ表示
- DiaryEditor に `defaultTab="edit"` を明示

- [ ] **Step 2: 型チェック**

```bash
pnpm exec tsc --noEmit
```

Expected: zero errors.

- [ ] **Step 3: コミット**

```bash
git add app/page.tsx
git commit -m "feat: show streak badge on home page based on consecutive diary days"
```

---

## Task 9: E2E スモークテスト + push

**目的:** 全機能を通しで触ってから GitHub に反映する。

**Files:** なし（実行のみ）

- [ ] **Step 1: シードでデータ準備**

```bash
pnpm db:seed:reset
pnpm db:seed
```

Expected: 7 件の日記が `今日から6日前 〜 今日` の範囲で入る。

- [ ] **Step 2: dev サーバー起動**

```bash
( pnpm dev > /tmp/diary-cal.log 2>&1 & )
sleep 8
grep -E "Ready in|started server on" /tmp/diary-cal.log
```

- [ ] **Step 3: streak 表示確認（`/`）**

```bash
curl -s http://localhost:3000 | grep -oE '🔥 [0-9]+日連続記入中' | head -2
```

Expected: `🔥 7日連続記入中` 系の文字列が見える（シード後で今日含めて 7 連続のはず）。

- [ ] **Step 4: 編集/プレビュー タブ確認**

```bash
curl -s http://localhost:3000 | grep -oE '(<button [^>]*role="tab"[^>]*>[^<]+</button>|編集|プレビュー)' | head -8
```

Expected: 編集 / プレビュー のタブ trigger が見える。

- [ ] **Step 5: バックデート動線確認（カレンダーで過去未記入セルがリンク）**

シード後はカレンダーで `01〜今日-7日前` が未記入の過去セル。それがリンクになっているか:

```bash
curl -s "http://localhost:3000/history?ym=2026-06" | grep -oE 'href="/diary/2026-06-01"' | head -2
```

Expected: `href="/diary/2026-06-01"` が見える（過去未記入セルがリンク化されている）。

- [ ] **Step 6: バックデート編集 動作確認**

過去未記入日 `/diary/2026-06-01` を開いたとき、空エディタが返ること:

```bash
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/diary/2026-06-01"
curl -s "http://localhost:3000/diary/2026-06-01" | grep -oE '(<h1[^>]*>2026[^<]*</h1>|編集|プレビュー)' | head -5
```

Expected: HTTP 200。h1 に `2026-06-01` が見える。タブも見える。

- [ ] **Step 7: 未来日付は 404**

```bash
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/diary/2030-01-01"
```

Expected: `404`.

- [ ] **Step 8: 既存 entry の詳細ページが preview タブ既定か**

```bash
curl -s "http://localhost:3000/diary/$(TZ=Asia/Tokyo date +%Y-%m-%d)" | grep -oE 'data-state="active"[^>]*>(編集|プレビュー)' | head -3
```

> このコマンドは Tabs の active 状態を取得する試み。失敗しても致命的でない（クライアントで描画する場合 SSR 時点で active 状態が分からないことがある）。実際の動作は実機ブラウザで確認。

代わりにシンプルなチェック:
```bash
curl -s "http://localhost:3000/diary/$(TZ=Asia/Tokyo date +%Y-%m-%d)" | grep -oE '(編集|プレビュー)' | head -5
```

Expected: 編集 / プレビュー の両方の文字列が見える（タブ trigger の表示）。

- [ ] **Step 9: Markdown プレビュー — DB に直接 Markdown 入りで上書きしてプレビュータブのレンダリングが期待通りか**

```bash
pnpm exec tsx --env-file=.env.local -e "
(async () => {
  const { Pool } = await import('@neondatabase/serverless');
  const p = new Pool({ connectionString: process.env.DATABASE_URL });
  const today = new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Tokyo' });
  await p.query(
    \`INSERT INTO diary_entries (user_id, entry_date, content) VALUES (\$1, \$2, \$3)
     ON CONFLICT (user_id, entry_date) DO UPDATE SET content = EXCLUDED.content, updated_at = now()\`,
    ['me', today, '# 今日のタイトル\n\n**太字**と*斜体*のテスト。\n\n- リスト1\n- リスト2']
  );
  console.log('Inserted markdown entry for', today);
  await p.end();
})();
"
```

その後、詳細ページに Markdown 由来の HTML が含まれるか確認:

```bash
TODAY=$(TZ=Asia/Tokyo date +%Y-%m-%d)
curl -s "http://localhost:3000/diary/$TODAY" | grep -oE '(<h1[^>]*>今日のタイトル|<strong>太字</strong>|<em>斜体</em>|<ul>)' | head -5
```

Expected: 少なくとも `<strong>太字</strong>` と `<em>斜体</em>` が見える（preview タブの SSR 時点で描画されているはず）。`<h1>今日のタイトル` も含まれていれば理想。

> 注: Tabs の inactive content は Radix/Base UI のデフォルトでは DOM にあるが `data-state="inactive"` で隠される。SSR 時点で preview タブが active のためコンテンツが見える想定。

- [ ] **Step 10: 削除動作（DB 直確認）**

ブラウザでクリックする代わりに、Server Action と同等の DB 状態を直接作って確認:

```bash
pnpm exec tsx --env-file=.env.local -e "
(async () => {
  const { Pool } = await import('@neondatabase/serverless');
  const p = new Pool({ connectionString: process.env.DATABASE_URL });
  const today = new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Tokyo' });
  const before = await p.query(\"SELECT count(*) FROM diary_entries WHERE user_id='me'\");
  console.log('Before:', before.rows[0].count);
  await p.query(\"DELETE FROM diary_entries WHERE user_id='me' AND entry_date=\$1\", [today]);
  const after = await p.query(\"SELECT count(*) FROM diary_entries WHERE user_id='me'\");
  console.log('After:', after.rows[0].count);
  await p.end();
})();
"
```

Expected: Before 7、After 6。

- [ ] **Step 11: dev サーバー停止**

```bash
pkill -f "next dev" 2>/dev/null
pkill -f "next-server" 2>/dev/null
sleep 2
```

- [ ] **Step 12: テストデータの掃除**

```bash
pnpm db:seed:reset
```

Expected: `Deleted ... mbti_snapshots, ... weekly_insights and ... diary_entries`。

- [ ] **Step 13: GitHub に push**

```bash
git push
```

Expected: M1.5 の 8 タスク分のコミットが origin/main に反映される。

---

## 完了基準

1. `/` で「🔥 N 日連続記入中」が出る（シード後）
2. DiaryEditor で「編集 / プレビュー」のタブが見え、プレビュータブで Markdown が描画される
3. `/history` のカレンダーで過去未記入日がクリックでき、`/diary/[date]` で空エディタが開く
4. 既存日記の `/diary/[date]` でプレビュータブ既定
5. 未来日付は 404
6. 削除ボタンが既存日記にだけ表示され、クリックで削除→`/` に遷移
7. `pnpm exec tsc --noEmit` がエラーゼロ
8. すべてのコミットが GitHub に push されている

---

## トラブルシューティング

| 症状 | 対処 |
|---|---|
| `shadcn add tabs` が `base-nova` で見つからない | `pnpm dlx shadcn@latest add tabs --overwrite` で再試行。ダメなら手動で Radix Tabs を install して書く |
| Markdown が見栄え悪い | `app/globals.css` の `@plugin "@tailwindcss/typography";` が `@import "tailwindcss";` より後にあるか確認 |
| プレビュータブで日記の改行が消える | これは仕様（CommonMark の段落区切りは空行が必要）。M1.5 では `remark-breaks` を入れない方針 |
| 削除ボタンを押しても遷移しない | Console 確認、`useRouter` import、`router.push('/')` が呼ばれているか |
| Backdate で過去未記入日に進めない | `DiaryCalendar.tsx` の DayCell ロジックで Link 化されているか、`isFuture` 判定が逆になっていないか |
| `prose` クラスが効かない | `@tailwindcss/typography` がインストール済みか、`globals.css` に `@plugin` 宣言があるか |
| Tabs で textarea の入力が消える | hidden input が `<form>` 内に置かれているか確認 |
| Button の `variant="destructive"` が型エラー | base-nova の Button にこの variant があるか確認。なければ `className` で直接スタイル |

---

## このプランで意図的に**やらないこと**

- shadcn Dialog による削除確認モーダル → ブラウザ `confirm()` で十分
- `remark-breaks` 追加 → Markdown 標準の改行挙動を尊重
- `react-markdown` の `components` カスタマイズ → 標準スタイルで十分
- Markdown 画像対応 → ストレージ問題が別件
- ベスト streak 記録の永続化 → モチベ用には現在 streak で十分
- shadcn add tabs 以外の component 追加 → 必要最小限
- `lib/db/queries/diary.ts` の `listDiaryDates()` 新設 → 当面エントリー数少ないので既存 `listDiaryEntries` で十分
